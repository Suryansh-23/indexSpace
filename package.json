{
  "name": "fs-sdk-starter-kit",
  "private": true,
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "packages/*",
    "indexspace/shared",
    "indexspace/backend",
    "indexspace/ui"
  ],
  "scripts": {
    "sync-env": "node scripts/sync-env.mjs",
    "dev": "node scripts/sync-env.mjs && concurrently --names backend,ui --prefix-colors cyan,blue 'cd indexspace/backend && MOCK_VAULT=true bun run dev' 'cd indexspace/ui && bun run dev'",
    "dev:real": "node scripts/sync-env.mjs && concurrently --names backend,ui --prefix-colors cyan,blue 'cd indexspace/backend && bun run dev' 'cd indexspace/ui && bun run dev'",
    "backend": "cd indexspace/backend && MOCK_VAULT=true bun run dev",
    "backend:real": "cd indexspace/backend && bun run dev",
    "ui": "cd indexspace/ui && bun run dev",
    "stop": "node scripts/stop-dev.mjs",
    "build": "cd indexspace/ui && bun run build",
    "typecheck": "cd indexspace/ui && npx tsc --noEmit",
    "screenshots": "cd indexspace/ui && bun run dev & sleep 4 && node indexspace/scripts/screenshots-desktop.mjs; kill %1 2>/dev/null"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.2.13",
    "@types/react-dom": "^19.2.3",
    "concurrently": "^9.2.1",
    "jsdom": "^25.0.1",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "vitest-axe": "^0.1.0"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
